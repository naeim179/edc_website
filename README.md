# الملفات المحوّلة إلى Next.js

انسخ الملفات الثلاثة الموجودة داخل مجلد `app` إلى مجلد `app` داخل مشروعك:

- `page.tsx`
- `globals.css`
- `layout.tsx`

قبل الاستبدال، احتفظ بنسخة من ملفات مشروعك الحالية.

بعد النسخ شغّل:

```powershell
npm run dev
```

ثم افتح:

`http://localhost:3000`
